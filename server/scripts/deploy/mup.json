{
  //server authentication info
  "servers": [
    {
      "host": "23.239.25.56",
      "username": "root",
      //"password": "password"
      //or pem file (ssh based authentication)
      "pem": "~/.ssh/id_rsa"
    }
  ],

  //install MongoDB in the server, does not destroy local mongo db on future setup
  "setupMongo": false,

  //WARNING: Nodejs is required! Only skip if you already have nodejs installed on server.
  "setupNode": false,

  //WARNING: If nodeVersion omitted will setup 0.10.25 by default. Do not use v, only version number.
  "nodeVersion": "0.10.28",

  //install PhantomJS in the server
  "setupPhantom": true,

  //application name
  "appName": "timelined",

  //location of app (local directory)
  "app": "/root/deploy/timelined",

  //configure environmental
  "env": {
    "ROOT_URL": "http://timelined.com",
    "PORT": "3000",
    "MAIL_URL": "smtp://postmaster%40timelined.com:99ouf5m6ccd5@smtp.mailgun.org:587"
  },

  //meteor-up checks if the app comes online just after the deployment
  //before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 15
}
